# Backend Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the Express server (default: 3002)
PORT=3002

# Environment mode: development | production
NODE_ENV=development

# =============================================================================
# CORS Configuration
# =============================================================================

# Frontend URL for CORS (default: http://localhost:5173)
CORS_ORIGIN=http://localhost:5173

# =============================================================================
# AI Services - REQUIRED
# =============================================================================

# Gemini API Key - REQUIRED for AI features (speech-to-text, script generation, etc.)
# Get your API key from: https://makersuite.google.com/app/apikey
# IMPORTANT: Never commit this file with your actual API key
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# Google Cloud Platform - Optional for development, required for production
# =============================================================================

# Google Cloud Project ID
# Required for: Firestore, Cloud TTS, Cloud Storage
# Find it at: https://console.cloud.google.com/home/dashboard
GOOGLE_CLOUD_PROJECT=your_project_id_here

# GCS Bucket Name for topic data storage
# Required for: Storing full topic data (slides) in production
# Create a bucket at: https://console.cloud.google.com/storage/browser
GCS_BUCKET_NAME=your_bucket_name_here

# =============================================================================
# Local Storage Options (Development Only)
# =============================================================================

# Enable local file storage for slides instead of GCS (development only)
# Set to '1' to store slides locally in ./data/slides/
# SHOWME_LOCAL_SLIDES=1

# Custom path for local slides storage (optional)
# SHOWME_LOCAL_SLIDES_DIR=/path/to/slides

# Alternative GCS bucket for slides (if different from GCS_BUCKET_NAME)
# SHOWME_GCS_BUCKET=your_slides_bucket_name

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level: debug | info | warn | error | none
# Default: 'info' in development, 'warn' in production
# LOG_LEVEL=debug

# Log categories filter (comma-separated)
# Options: API, WS, GEMINI, STATE, GENERATION, PERF, GRAPH, STORAGE, PROGRESS
# Use '*' for all categories
# LOG_CATEGORIES=*

# Log file path (default: /tmp/showme-server.log)
# LOG_FILE=/tmp/showme-server.log
