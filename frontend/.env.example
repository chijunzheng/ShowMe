# Frontend Environment Variables
# Copy this file to .env and fill in your values

# Backend API URL (for production builds)
VITE_API_URL=http://localhost:3001

# WebSocket URL (for production builds)
VITE_WS_URL=ws://localhost:3001

# IMPORTANT: Do NOT add any API keys or secrets here
# All API keys should remain on the backend only
# Vite exposes all VITE_* variables to the browser
